name: poc_engine
description: Proof of Culture Mobile App
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.1.0
  qr_code_scanner: ^1.0.1
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0

  cupertino_icons: ^1.0.6

flutter:
  uses-material-design: true
