{
  "name": "poc-monad-contracts",
  "version": "1.0.0",
  "description": "Proof of Culture smart contracts deployed on Monad",
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run deploy/deploy.ts --network monadTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-viem": "^3.0.2",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^16.6.1",
    "hardhat": "^3.1.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "solc": "^0.5.0"
  }
}
